services:
  bobo-bot:
    build: .
    container_name: bobo-bot
    restart: unless-stopped
    volumes:
      # 持久化 config（auth.json、bobo.db 等）和 assets
      - ./config:/app/config
      - ./assets:/app/assets
    environment:
      - NODE_OPTIONS=--experimental-sqlite
